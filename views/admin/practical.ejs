<%- include("partials/header"); -%>
    <style>
        .edit-delete-btn button{
            font-size: 1rem;
            color: #367D43;
            border-radius: 5px;
            padding: 10px;
            padding-left: 25px;
            padding-right: 25px;
            margin: 1rem;
            border: none;
            background-color: #cadbc2;
            box-shadow: 3px 3px 5px rgb(78, 77, 77);
        }
        .edit-delete-btn button:hover{
            font-size: 1.1rem;
            color: #cadbc2;
            background-color: #367D43;
            box-shadow: 3px 3px 5px #cadbc2;
            cursor: pointer;
        }
        #title{
            font-size: 3rem;
            color: #367D43;
            margin: 3rem;;

        }
        #container{
            text-align: center;
        }

        #practical{
            min-height: 875px; 
            max-width: 1300px; 
            margin: 2%;
            margin: auto;
            margin-top: 10px;
            margin-bottom: 10px;

        }
        
        .ql-toolbar{
            display: none;
        }
    </style>

    
    <div id="container">
        <h1 id="title">
            <%=title%>
        </h1>
        <div id="practical">
        
        </div>
        <div class="edit-delete-btn">
            
            <button onclick="openEditor()">
                <i class="fa-solid fa-pen-to-square"></i> 
                Edit
            </button>

            <button onclick= "deletePractical()"><i class="fa-solid fa-trash-can"></i> Delete</button>
        </div>
    </div>
    

    <script src="https://cdn.quilljs.com/1.3.6/quill.min.js"></script>
    <script src="https://unpkg.com/axios/dist/axios.min.js"></script>
    <script src="/js/practical.js"></script>

    <script type="text/javascript">

        const openEditor = ()=>{
            window.location.href = '/admin/<%=department%>/<%=year%>/<%=id%>/edit'
        } 

        const deletePractical = async ()=>{
            await axios.delete ('/admin/<%=department%>/<%=year%>/<%=id%>/delete' )
            window.location.href = '/admin/virtual-lab/<%=department%>/<%=year%>'
        }
        

        // Putting quill data comming from server to the quill container
            const responseData = <%- JSON.stringify(data) %>;
            console.log(responseData)
            quill.setContents(responseData); 
    </script>



    <%- include("partials/footer"); -%>
